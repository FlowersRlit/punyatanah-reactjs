import React from "react";

const FooterColsTitle = ({ Title }) => {
  return (
    <h6 className="mb-4 flex justify-center font-semibold uppercase md:justify-start">
      {Title}
    </h6>
  );
};

export default FooterColsTitle;
